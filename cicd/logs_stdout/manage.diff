--- manage	2022-10-27 17:58:25.000000000 +0200
+++ manage.new	2022-11-22 15:32:23.724457248 +0100
@@ -51,5 +51,5 @@
 start()
 {
   echo "starting $ME"
-  LD_PRELOAD=$JEMALLOC_ROOT/lib/libjemalloc.so wmc-httpd -r -d $STATEDIR -l "|rotatelogs $LOGDIR/crabserver-%Y%m%d-`hostname -s`.log 86400" $CFGFILE
+  LD_PRELOAD=$JEMALLOC_ROOT/lib/libjemalloc.so wmc-httpd -r -d $STATEDIR -l "/data/srv/state/crabserver" $CFGFILE
 }

 # Stop the service.
